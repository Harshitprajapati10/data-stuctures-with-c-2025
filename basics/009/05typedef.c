#include <stdio.h>

int main(){
    // typedef is used to used own defined datatype, typedef is used to create an alias name for data types in c, generally used with structures,
    // typedef -> naya datatype bana lo
    typedef int harsh;
    harsh a = 6;
    printf("The value of a in %d \n",a);
    return 0;
}